import DockManager from "@/components/DockManager";

export default function DockManagerPage() {
  return (
    <div className="h-screen w-full overflow-hidden">
      <DockManager />
    </div>
  );
}
